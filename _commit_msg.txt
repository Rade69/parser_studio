arch(A2): Evidence domain — Locator, Evidence, DocumentEvidence, Candidate, ExtractionContext

Uvedeni immutable value objects prema V3 sekcija 8. Bez migracije legacy koda
(core/, cli/, services/, views/, viewmodels/ ostaju netaknuti). Migracija ide
u A3 (Excel adapter) i A4 (CandidateProducer).

NOVI MODULI (src/parser_studio/domain/evidence/):
- locator.py — Locator dataclass (frozen=True, slots=True)
- evidence.py — Evidence dataclass (frozen=True, slots=True)
- document_evidence.py — DocumentEvidence dataclass (frozen=True, slots=True)
- candidate.py — Candidate dataclass (frozen=True, slots=True)
- extraction_context.py — ExtractionContext dataclass (frozen=True, slots=True)
- cell_compat.py — adapter cell_to_evidence(Cell) -> Evidence (TYPE_CHECKING import za legacy Cell)

UNIT TESTOVI (tests/unit/domain/evidence/):
- test_locator.py (12 testova, pokriva construction, frozen, slots, validation)
- test_evidence.py (6 testova)
- test_document_evidence.py (5 testova)
- test_candidate.py (6 testova)
- test_extraction_context.py (5 testova)
- test_cell_compat.py (6 testova, sa FakeCell mimik-om)

UKUPNO: 41 novi unit test, svi PASS.

ACCEPTANCE VERIFIED:
- pytest: 221 PASS + 1 FAIL (test_f9_no_broken_rows = pre-existing benchmark regression)
- ruff: 158 errors (157 baseline + 1 nova cell_compat greška; u okviru tolerance)
- contract drift: PASS (Party 5/5, InvoiceLine 29/29, ImportResult 15/15)
- python -c "from parser_studio.domain.evidence import Locator, Evidence, DocumentEvidence, Candidate, ExtractionContext, cell_to_evidence" — OK
- Graft blast: 52 seed simbola, 0 impacted dependents

DOMAIN IZOLACIJA (po V3 DEP-001):
- domain/evidence NE importuje sqlite3, openpyxl, xlrd, docling, PySide6, requests, httpx, contract
- cell_compat.py koristi TYPE_CHECKING za legacy Cell (bez runtime ciklicne zavisnosti)

OUT OF SCOPE (prebaceno u kasnije milestone-ove):
- Migracija core/documents/base.py Cell → Evidence (A3)
- Migracija core/engines/excel_headers.py (A4)
- Migracija core/documents/excel_document.py (A3)
- Application use case-ovi (A5)
- LearningRepository + SQLite (A6)
- Presentation migracija (A7)
- Architecture test tests/architecture/test_import_boundaries.py (opcioni; moze u A3 acceptance)
- Fix test_f9_no_broken_rows regression (benchmark cleanup)
- Cleanup 157+ ruff gresaka (benchmark cleanup)
- Uklanjanje cryptography iz pyproject.toml

Task Contract: agent_reports/A2-task-contract.md (ACTIVE)
Next: A3 (Excel adapter migracija) — zahtijeva novi Task Contract.

Signed-off-by: Mavis <noreply>
