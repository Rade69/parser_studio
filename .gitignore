__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
.pytest_cache/
.ruff_cache/

# ── KRITICNO: stvarne fakture sadrze poslovne podatke (naziv firme, JIB,
# adresa, broj fakture) i NIKAD ne smiju u git istoriju.
*.psproj/samples/
*.psproj/expected/
*.psproj/out/
tests/fixtures/real/
tests/fakture/
tests/fakture_ground_truth/
samples/

# ── Benchmark eksperiment (Docling vs PaddleOCR) — izolovani env i outputi
experiments/document_engines/docling_env/
experiments/document_engines/paddle_env/
experiments/document_engines/paddle_env_3_0_0/
experiments/document_engines/outputs/
experiments/document_engines/models/
experiments/document_engines/quality_v3/
# benchmark/ skripte i lock fajlovi SE PRATE (nisu u ignore)

# Privatni kljuc za potpisivanje parsera - nikad u repo
*.pem
*.key
signing_key*

# Lokalna konfiguracija (putanje do deklarant_pro, do kljuca)
config.local.json
.env

# graft's local graph cache — regenerable, not committed (run `graft build`).
/graft/
